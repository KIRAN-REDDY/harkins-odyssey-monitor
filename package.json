{
  "name": "harkins-odyssey-seat-monitor",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "Fail-closed cloud Playwright monitor that verifies Harkins seat availability from live API data or rendered seat icons.",
  "scripts": {
    "monitor": "node src/monitor.mjs",
    "inspect": "node src/inspect.mjs",
    "test": "node --test src/*.test.mjs",
    "test-alert": "node src/test-alert.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "playwright": "1.61.1"
  }
}
